<idea-plugin>
    <name>Jenkins Control Plugin</name>
    <description>A Jenkins Plugin for Intellij for IDEA 10+</description>
    <version>0.5.2</version>
    <vendor email="david.boissier [at] gmail [dot] com" url="http://codinjutsu.blogspot.com">David Boissier</vendor>
    <idea-version since-build="107.587"/>

    <depends>com.intellij.modules.lang</depends>

    <project-components>
        <component>
            <implementation-class>org.codinjutsu.tools.jenkins.JenkinsControlComponent</implementation-class>
        </component>
    </project-components>

    <change-notes><![CDATA[
<ul>Version 0.5.2
    <li>[Add] The plugin starts up with the last selected view</li>
    <li>[Add] Widget : when clicking on the status icon, a popup is displayed with builds status summary info</li>
    <li>[Upd] Job are loaded in background (with a waiting UI decorator)</li>
    <li>[Upd] Improvement in handling Connection Exception</li>
    <li>[Fix] Widget should not be duplicated when opening new Project Window</li>
</ul>
<ul>Version 0.5.1
    <li>[Fix] Regression : When the server url is not set (or set to a dummy url), the plugin should not try to connect</li>
    <li>[Fix] Browser Panel : OneTouch Expandable should be accessible</li>
</ul>
<ul>Version 0.5.0
    <li>[Add] Job Tree : Search job panel (CTRL + F, F3 to search forward, SHIFT + F3 to search backward, the search feature is cyclic)</li>
    <li>[Add] Jenkins Widget that displays either a blue icon (no broken build on selected view) or else a red icon with the number of the remaining broken builds</li>
    <li>[Upd] Increase loading performance</li>
    <li>[Fix] Connection issue when Job and Rss autorefresh are both enabled</li>
    <li>[Fix] Configuration Panel: File input fields should have a validator</li>
    <li>[Fix] Configuration Panel: Rename 'Password' to 'Password file'</li>
</ul>
]]> </change-notes>
</idea-plugin>
